# Tooling & environment

- Use mise-en-place to add tasks (e.g. run prek hooks, serve docs, run linting), environment variables (e.g. default values for CLI options) and tools pinning (e.g. prek or uv).
- Use uv for everything related to Python. "uv add" for dependencies, "uv run" to run things, "uvx" to run tools that are not installed locally, "uv tree" to understand dependencies, etc.
- Use ty for type checking.
- Use ruff for formatting.
- Use pytest-sugar for unit tests.
- Use prek for pre-commit hooks.

# Python

- If coding in Python, use the oldest supported version (https://devguide.python.org/versions/#supported-versions).
- Use loguru for logging, Typer for CLI tools, rich for nice formatting.
- Avoid reshape, view, unsqueeze, squeeze, transpose, etc. in favor of einops.
- When possible, use jaxtyping to annotate parameters. Avoid simply "torch.Tensor" or "nd.array".

# Code style

- Finish all files with a new line.
- Do not set a limit for line length.
- If the arguments of a function are in a different line when calling or
  defining it, add a trailing comma after the last argument so that the
  formatter splits (one line per argument) or just split yourself.
- Use underscores for private methods, attributes, etc.
- If a string is split into multiple lines, prefer spaces at the beginning of
  a line instead of at the end. E.g., "This is the first line" " and this is
  the second".
- Don't use parentheses or tuples unless needed. Bad: `x = (1, 2)`. Good: `x = (1,)`, `x = 1, 2`.

# Testing

- When possible, use test-driven development. Write first the test, then the implementation.

# Documentation

- Use Zensical for documentation. Zensical docs are at https://zensical.org/docs/get-started/.
- When writing documentation, follow the philosophy of https://diataxis.fr/.
- Use Typst if you need to write scientific reports or slides.
- When building a workflow for documentation, use Smokeshow for ephemeral deployments, adding a comment in the PR with the deployment URL.
- Use Google style for docstrings.
- Add Google-style docstrings to all new classes, methods and functions.
- Add usage examples in docstrings of complex objects (e.g. with non-trivial
  parameters).
- Make sure the docstrings formatting uses mkdocstrings style, not Sphinx (e.g.
  single vs double backticks, directives like :attr:), unless the current project uses Sphinx. NEVER USE DOUBLE BACKTICKS for mkdocstrings docstrings.
- Don't hard-code things in the docs that will need maintenance, e.g., "there are 25 transforms available".
- If you are making diagrams in docs, use Mermaid, not ASCII.

# Git & collaboration

- Do not add yourself as contributor to Git commits.
- When writing a comment, issue or PR description, if it's under my username,
  prefix the text with "[Generated by a coding agent]\n\n---\n\n"
- Always ask for confirmation before deleting files/folders.
- Do not run `rm` until I've given explicit permission.
- Document all you've done in a markdown file in the current folder (copilot_log.md). Particularly important are things that failed, and gotchas. Never stage nor commit that file with git.

# Replying to PR review comments

- Reply to each review comment individually, on its own thread.
- Prefix every reply with the AI disclosure: "[Generated by a coding agent]\n\n---\n\n".
- Acknowledge the reviewer's point specifically — never a bare "Thanks".
- Explain the decision and rationale, including any deviation from the
  reviewer's suggestion and why.
- Make one self-contained commit per comment where feasible; push before
  replying so the hash and links resolve on GitHub.
- Include a commit-pinned GitHub permalink to the new code (a
  `.../blob/<sha>/<path>#Lstart-Lend` URL on its own line — it renders as a
  code snippet).
- End with "Addressed in <commit-sha>." citing the exact fixing commit.
